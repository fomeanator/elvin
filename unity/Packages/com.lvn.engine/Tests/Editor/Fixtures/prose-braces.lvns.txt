scene prose_braces

# Многострочная проза «…» — не код. Её фигурные скобки это подстановка или
# просто знаки: `}` в такой строке не закрывает блок, а `func` в ней не
# объявляет функцию. Разбор шёл по сырому тексту, и такая глава роняла сборку
# в редакторе, хотя через lvnconv собиралась.
:start
set key=hp value=7
«Записка на столе, почерк торопливый:
он писал } в конце каждой строки, будто закрывал двери.
А ниже — func и цифра {hp}, обведённая дважды.»
Следователь: Почерк тот же.
