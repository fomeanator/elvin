scene include_nested

// ЦЕПОЧКА ПОДКЛЮЧЕНИЙ. Разворот идёт ДО разбора и РЕКУРСИВНО: подключённый
// файл сам подключает следующий, и путь во втором звене считается от него,
// а не от главы. Однопроходный разворот показал бы игроку строку
// `include "chain-b.inc.txt"` как реплику — ровно тот молчаливый отказ, ради
// которого гейт и собирает фикстуру ФАЙЛОМ, а не текстом.

Аня: Начали.
-> middle

include "Nested/chain-a.inc.txt"
