scene func_oneline

# Объявление функции в ОДНУ строку — законная запись. Редакторный путь читал
# сырой текст и такого объявления не видел вовсе: число доводов не сверялось, а
# строка вызова не разворачивалась, командой не была и уходила в НАРРАЦИЮ —
# то есть печаталась игроку. Через lvnconv та же глава играла правильно.
func bow(who) { Слуга: Кланяюсь, {who}. }

func tally(a, b) { __ret = a + b }

:start
bow("сударь")
total = tally(2, 3)
Счетовод: Итого {total}.
