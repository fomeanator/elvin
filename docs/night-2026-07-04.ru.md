# Ночная смена 3–4 июля: что сделано и как проверено

*Сводка для утреннего разбора. Всё в локальных коммитах на main (сеть до
GitHub ночью флапала — дожать пуш: `git push origin main feat/import-autocast`).*

## Релиз

| Что | Как проверено |
|---|---|
| **v0.7.0 выпущен**: тег → CI → APK на GitHub Release | Джоба Succeeded; `sovet-demo-v0.7.0.apk` (104 МБ) в релизе и в `~/ominis/builds/` |

## Движок (кандидаты в v0.8.0 — CHANGELOG уже полон)

| Что | Как проверено |
|---|---|
| ⏱ **Таймер выбора** — `choice timeout=5 timeout_goto=late`, полоса отсчёта, заморозка в меню/арт-вью | EditMode 4 теста + PlayMode-смоук (истечение уводит в ветку); грамматика/конвертер/валидатор/wasm |
| ⌨️ **Ввод текста** — `input var=… prompt=…`, оверлей, Enter/OK | PlayMode-смоук: ввод → `{name}` интерполируется; конвертер+валидатор тесты |
| 🎙 **Озвучка** — `voice "<url>"` перед репликой, авто-стоп, слайдер «Озвучка», глушение тайп-блипа | EditMode-тест проброса; конвертер-тест; generation guard от поздних загрузок |
| Демо: глава 4 «Викторина на время»; кукла и квиз озвучены (TTS Milena, 10 wav) | `convert`+`validate` 0 warnings; сервер отдаёт главу |
| Контент **−22 МБ**: 125 PNG-под-маской-.jpg → настоящий JPEG q87; 176 PNG lossless | Скрипт с альфа-гардом; фоны отсмотрены глазами (2 шт., чисто) |
| Валидатор: `on_drop`/`on_drop_miss` = jump-цели; `input` без `var` = ошибка; таймер-половинки = warning | Go-тесты; реальная находка на doll-drag.lvns |
| CI: upload-artifact нефатален (таймауты сети красили зелёные прогоны) | Разбор Worker-лога упавшей джобы f2918f6 |

## Стратегия «язык века ИИ» (карт-бланш)

| Что | Как проверено |
|---|---|
| **docs/language-strategy.ru.md** — аналоги (Ren'Py/Ink/Twine/PICO-8/Godot), общие черты выживших, козырь ИИ-века, приоритеты | — (стратегический док) |
| **llms.txt** (корень + `/llms.txt`) — карта доков для агентов | Все ссылки проверены на существование |
| **MCP-сервер** `lvn-mcp`: `lvns_check`/`lvns_convert`/`lvn_doc` | 7 протокол-тестов (initialize/list/call, чистый и битый скрипт, доки) |
| **howto/TUTORIAL.md** — «первая игра за 15 минут» + Шаг 0 | Пример скомпилирован (0 warnings); флоу панели сверен с кодом |
| README освежён до v0.7 (playground, MCP, AI-пути) | — |

## Playground `/play/` (главный ночной кусок)

| Что | Как проверено |
|---|---|
| Слева `.lvns` — справа играет; **настоящий компилятор в wasm** | CDP e2e: ввод имени → интерполяция; фильтры лавки по золоту; таймаут-ветка сработала сама во время скриншота |
| **Share-ссылка** (скрипт в URL-хеше) | Открытие хеш-ссылки грузит скрипт |
| **Подсветка синтаксиса** (backdrop-pre, без Monaco) | Скриншот: команды/метки/спикеры/строки/интерполяция в цветах |
| **Каталог манифеста**: слоёные актёры, `{emotion}`-оси | Codel живьём: neutral→happy по ходу сцены; кукла из 4 слоёв с гео |
| **⬇ HTML — один самодостаточный файл**: арт как data-URL, каталог внутри | Открыт с `file://` полностью офлайн: Codel с эмоциями, все src=data: |
| **Сейвы** (playground и экспорт): автосейв на паузе, «Продолжить/Заново», чистка на финале | e2e: купил яблоко → перезапуск → Продолжить → «Свежее!», HUD «💰 9 🎒 яблоко»; то же в file:// |
| Live-lint при вводе, черновик в localStorage, ⬇ .lvn, мобильная вёрстка | Скриншот 390×844: тулбар обёрнут, кнопки пальце-пригодны |
| Сервер: `Cache-Control: no-cache` для `/play/*` | Module-map браузера переживал даже ignoreCache-reload — лечится только заголовком |

## После сводки (юзер проснулся, «го»)

| Что | Как проверено |
|---|---|
| **Рыцарь проходим** — цикл стал выбором «Ещё потаскаю / Занавес», глава 4 разблокирована | Перекомпилирован; сервер отдаёт; последние опы: label/say/choice |
| Playground: **rollback** (↩ + колёсико) и в экспортном HTML тоже; sfx-ваншоты | Живьём: откат с реплики возвращает input-оверлей |
| Экспорт: **тайтл-экран** (имя + «Играть» = жест для аудио-автоплея) | Живьём в file://: карта → Играть → resume-оффер |
| **Продуктовые сервисы**: device-auth (идемпотентная регистрация, ротация токена), кошелёк (earn/spend/409/sku-грант, аудит-история), IAP по каталогу (-iap-dev; в проде честный 501), аналитика (JSONL-дни, батчи, admin summary) + Unity-клиенты LvnBackend/LvnWallet/LvnAnalytics (офлайн-деградация, персистентная очередь) | 4 httptest-сьюта; живой e2e на :8077 (регистрация→50→+550 IAP→трата 600 с мечом→summary); EditMode 358/358; docs/services.md |

| **Daily-бонус**: стрик по UTC-дням, сброс после пропуска, награды из daily-rewards.json через кошелёк | Time-machine тест (рост/сброс/409 + кошелёк получил 3 гранта); e2e: claim 25 gold → 409 |
| **Лидерборды**: board-slug, best-score-wins, top-N + свой ранг | Тест (2 игрока, порядок, не-даунгрейд, traversal-гард); e2e: submit 420 → rank 1 |
| **NovelApp авто-аналитика**: boot/chapter_start/chapter_finish + авторегистрация девайса | EditMode 358/358; Track no-op без BaseUrl (офлайн-игры не копят очередь) |

| **ext-опы сервисов из .lvns**: wallet_earn/spend, leaderboard_submit (score_var/name_var), daily_claim, track; NovelApp регистрирует авто; квиз куклы сабмитит счёт | EditMode 358/358; рецепт в howto |
| **Браузер → сервисы**: playground (same-origin) минтит device-аккаунт и исполняет track/leaderboard_submit; викторина-пример = реальное соревнование по ссылке | Живьём: идеальный прогон → «ранг 1», борд показывает запись |
| Фикс: rune-safe обрезка имени в лидерборде (кириллица резалась посреди руны) | Регрессионный тест: 32 руны, без � |

## Тесты на момент сводки

EditMode **358/358**, PlayMode **8/8** (C# после этого не трогался),
Go — оба модуля зелёные (+MCP 7), node lang+playground **30/30**.

## Что предлагаю утром

1. Дожать пуши (одна команда выше) и посмотреть CI.
2. Потыкать playground руками: `localhost:8077/play/` (+ с телефона в
   локальной сети). Экспортнуть HTML и переслать кому-нибудь.
3. Телефон-тест APK v0.7.0 → если ок, тег **v0.8.0** (таймер+ввод+озвучка
   уедут партнёрам, APK станет на ~22 МБ легче).
