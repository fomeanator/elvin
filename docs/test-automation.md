# Лестница полной автоматизации тестирования

Стоячая цель (задана 2026-07-19): каждые ~5 минут делается ОДИН шаг к состоянию
«всё тестируется само»: от чистого плеера до бота, который сам тыкает APK.
Правила тиков: один шаг за тик; чекбокс закрывается только когда шаг реально
работает (прогон зелёный); коммит после каждого шага; редактор Unity закрыт;
дев-сервер юзера на :8078 не трогать (тестовый порт — :8099); партнёрский
контент (scripts/cold-*, art/, sprites/cold*) в git не попадает; прод — только
чтение. Полный EditMode-прогон ~3 мин: гонять через `-testFilter` пока идёт
разработка шага, полный — на закрытии шага.

Прогон EditMode: `Unity -batchmode -projectPath sandbox -runTests
-testPlatform EditMode -testResults r.xml -testFilter "<Fixture>"`.

## A. Соук-бот на чистом плеере (EditMode, без устройства)

- [x] A1. `SoakBotTests.cs`: случайное прохождение ВСЕХ `server/content/scripts/*.lvn`
      (83 скрипта; скип если контента нет) — случайные выборы (бесплатные опции),
      бюджет 2000 пауз, ноль исключений/ошибок. Сделано 2026-07-19.
- [x] A2. Resume-truth: Save → свежий плеер → Restore + ReplayVisuals → сцены
      равны. SceneModel вынесен в общий internal (SceneModel.cs). Плотность:
      первые 200 пауз каждая, дальше каждая 17-я — проверка на каждой паузе
      делала цикличные главы квадратичными (полчаса на фикстуру); с общим
      распарсенным LvnDocument весь соук — 35 секунд.
- [x] A3. Все главы + howto/showcase, 3 сида на скрипт; база сидов варьируется
      через LVN_SOAK_SEED_BASE (10 прогонов стабильности = 30 разных путей).
- [x] A4. Через `qa/run-all.sh`: полный EditMode (юнит+контракт+соук) одним
      входом; `--device` добавляет сборку dev-APK и смоук на эмуляторе.
      НАХОДКА СОУКА: 6 глав cold (ch14/17/18/21/23/24) — безусловный goto-цикл
      гардероба, выходное условие потеряно импортёром на articy-пине; история
      после цикла — мёртвый код; на устройстве шит переоткрывается вечно
      (вероятно, партнёрский «баг гардероба»). Известные — в KnownContentLoops
      соука (skip с инверсией). Чинить в импортёре (связность Jump/пины).

## B. «Тест на каждую функцию»: матрица опов

- [x] B1. Инвентаризация: перечислить все опы LvnPlayer/VnStage и отметить,
      какие уже покрыты. Сделано 2026-07-19, счёт = «в скольких тест-файлах
      Tests/Editor встречается оп»:

      | Оп | Файлов | | Оп | Файлов |
      |---|---|---|---|---|
      | say | 15 | | preload | 1 |
      | label | 13 | | audio/fade/dim/tint/particles/camera | 1 |
      | set | 8 | | **input** | **0 → закрыт InputAndLoadOpTests** |
      | goto | 8 | | **load** | **0 → закрыт InputAndLoadOpTests** |
      | choice | 8 | | **blur** | **0 (стейдж)** |
      | inc / if | 4 | | **obj / hotspot** | **0 (стейдж)** |
      | bg / actor | 3 | | **wallet_earn/spend** | **0 (ext)** |
      | call / return / wait | 2 | | **leaderboard_submit / daily_claim / ad_reward** | **0 (ext)** |

      Механизм кастом-опов покрыт (LvnOpsTests, ExtensionApiContractTests);
      track — 8 файлов.
- [x] B2. Закрыть непокрытые опы контракт-тестами:
      - [x] input, load — InputAndLoadOpTests.cs (2026-07-19)
      - [x] blur, obj, hotspot (= obj+on_click→GoTo, включая оживление
            завершённого плеера), ext-опы services оффлайн-путём —
            StageAndServiceOpTests.cs (2026-07-19)
- [ ] B3. Контракты выражений LvnExpression: база ЕСТЬ давно —
      LvnExpressionTests.cs, 17 тестов (арифметика, функции, rand/chance,
      коллекции, операторы). Остаток: вложенные global.* в выражениях
      (0 упоминаний в тестах) + паритет с playground expr.js.

## C. PlayMode headless (интеграция шелла)

- [x] C1. Ассембли Lvn.Engine.Tests.Runtime существовал (StageSmokeTests);
      добавлен BootSmokeTests: настоящий NovelApp в player loop.
- [x] C2 (ядро). BootSmokeTests поднимает НАСТОЯЩИЙ Go-сервер (qa/bin/
      lvnserver-test, собирает run-all; свободный порт) и доводит бут до
      «shell built» за ~2с. Грабли: (1) сервер стартует ~100мс — без
      ожидания healthz первая проба ловит refused и бут уходит в 5с-ретраи;
      (2) редиректнутые пайпы процесса надо дренировать (BeginOutputReadLine),
      иначе сервер блокируется на полном буфере; (3) Unity сегфолтится (139)
      на выходе из batchmode-PlayMode ПОСЛЕ записи XML — гейты читают XML,
      не код выхода. Полный цикл «глава→сейв→рестарт→резюм» в PlayMode —
      следующий уровень (EditMode-соук уже покрывает его логику глубоко).
- [x] C3. PlayMode-прогон в qa/run-all.sh и в ночной stability-джобе CI.

## D. Бот, который сам тыкает APK (headless-эмулятор)

- [x] D1. Быстрая версия: `qa/monkey.sh` — adb monkey по com.ominis.timeromance
      (500 событий, seed фиксирован) + захват logcat + грep крэшей; отчёт в файл.
      Сделано 2026-07-19: скрипт поднимает headless AVD, ставит APK, ждёт
      маркер `[lvn-boot] … veil handed off`, скриншотит, гоняет monkey,
      грепает крэш-сигнатуры; отчёты в qa/reports/ (в .gitignore).
      Прогон зелёный: бут 5.5с, 500 событий, monkey сам вошёл в главу.
      Грабли (вшиты в скрипт): (1) дефолтный для -no-window SwiftShader вешает
      Unity 6 до первого лога — нужен `-gpu host`; (2) с Vulkan-фичей эмулятора
      Unity 6 (Auto API) виснет на инициализации — нужен `-feature -Vulkan`
      (движок падает на GLES3); (3) оверлей «Viewing full screen» крадёт фокус
      → APP_CMD_LOST_FOCUS → Unity паузит цикл до первой сцены навсегда —
      лечится `settings put secure immersive_mode_confirmations confirmed`.
- [x] D1b. Переопределение сервера для тестового APK. Сделано 2026-07-19:
      LvnLaunchOverrides (shell) — intent extra `lvn_server` (Android) или env
      LVN_SERVER (десктоп/CI), ТОЛЬКО при Development Build (релиз глух —
      прод-APK не увести на чужой сервер); гейт покрыт LaunchOverrideTests.
      CliBuild: LVN_BUILD_DEV=1 → Development. Пакету shell добавлена
      зависимость com.unity.modules.androidjni (AndroidJavaClass в плеере).
      Прогон: dev-APK 38МБ → эмулятор → :8099 → online=True, 14 тайтлов,
      monkey 500 PASS. Грабля: 10.0.2.2 не добивал до хоста — monkey.sh сам
      ставит `adb reverse` для localhost-URL (работает и на физ. девайсе);
      смоук ассертит и применение override, и online=True (оффлайн-кэш прода
      переживает `install -r` и маскирует недостижимый сервер).
- [x] D2 (lite). `qa/story-walk.sh` — сюжетный бот БЕЗ AltTester: ставит APK,
      бутит, тапами входит в главу и «читает» историю (30 тапов); прогресс
      доказывается диффом скриншотов (10/10 уникальных кадров на LVN Tour
      против :8099), здоровье — крэш-грепом и живым процессом. Грабли:
      русская локаль printf ждёт запятую → координаты через awk; кнопка
      «Продолжить» на Y≈0.79. AltTester (полноценные сценарии «купи наряд,
      переустанови») — следующий уровень.
- [ ] D3. Сценарии партнёрских багов как скрипты (глава 2 рестарт, гардероб,
      имя). Частично: гардеробный баг УЖЕ ловится автоматически (соук +
      KnownContentLoops — детектор бесконечного цикла после импорта).

## E. CI

- [x] E1. Соук+матрица едут в unity-джобе CI автоматически (они в общем
      EditMode-прогоне). Починен гейт report: Skipped больше не считается
      провалом — на чистом чекауте раннера соук (нет партнёрского контента)
      и бут-смоук (нет qa/bin) легально скипаются.
- [x] E2 (подготовлено). ci.yml: schedule 02:30 UTC + джоба nightly-stability
      (qa/stability.sh 5, отчёты артефактом). Оживёт с первым git push.

## F. Один вход: «ничего не сломалось?» и «стабильно ли?»

- [x] F1. `qa/run-all.sh` — одна команда после изменений: полный EditMode
      (495 тестов: юнит + контракт + соук по 83 скриптам, ~2 мин вместе со
      стартом Unity); `--device` добавляет dev-APK + эмуляторный смоук против
      локального :8099; `--filter` для точечных прогонов. Выход 0/1 + отчёты.
- [x] F2. `qa/stability.sh N` — ловля флейков «раз на 10 отваливается»:
      N полных прогонов, каждый трясёт соук НОВЫМИ сидами (база 1000*i);
      сводка «тест → падений из N», различает ВСЕГДА (баг) и иногда (ФЛЕЙК).
- [x] F3. Прогнано 2026-07-19 (qa/stability.sh 10): 10/10 прогонов, ~34с
      каждый, сиды 1000..10000 — ФЛЕЙКОВ НЕТ, все тесты зелёные во всех
      прогонах (суммарно 2490 случайных соук-прохождений 83 скриптов).
      Отчёты: qa/reports/20260719-122422-stability.

## G. Следующий уровень (задан 2026-07-19 после закрытия A–F)

- [ ] G1. Импортёр: восстановить hide на слиянии веток — 673 слот-коллизии
      (ворклист = лог [slot-probe] SlotCollisionProbeTests) и потерянные
      условия выхода гардеробного цикла (6 глав в KnownContentLoops).
      После починки: переимпорт cold, зонд к нулю, KnownContentLoops пустеет
      (инверсия в соуке сама потребует убрать главы из списка).
- [ ] G2. Полный C2-цикл в PlayMode: бут → глава → сейв → рестарт → резюм
      с той же сценой (расширение BootSmokeTests).
- [ ] G3. Перф-бюджеты как тесты: бут до «shell built» с бюджетом времени в
      PlayMode-смоуке, полный EditMode ≤ 90с, соук ≤ 60с — регрессия времени
      = красный прогон (ловим деградацию до жалоб партнёров).
- [ ] G4. Мутационный соук: порча контента на лету (битые goto, отсутствующие
      поля, оборванный JSON) — движок обязан деградировать предсказуемо
      (скип/лог), а не падать; расширение «malformed must never crash».
- [ ] G5. D3-скрипты остальных партнёрских багов (глава 2 рестарт, имя) как
      story-walk сценарии с ассертами по logcat-маркерам.
- [ ] G6. AltTester (полный D2): инструментированный APK-таргет, сценарий
      «пройди 2 главы, купи наряд, переустанови, проверь прогресс».
- [ ] G7. Live-проверка ночной nightly-stability джобы после первого push
      (schedule уже в ci.yml, ждёт push).

## Журнал тиков

(время — шаг — результат)

- 2026-07-19 13:0x — внеплановый заход (юзер, скрин партнёра «два перса в
  слоте») — зонд потокового порядка SlotCollisionProbeTests нашёл 673
  коллизии (hide теряется на слиянии линеаризованных веток; файл-порядок
  чист); в VnStage добавлен слот-арбитр ArbitrateSlotX (видимый актёр владеет
  слотом, претендент разъезжается, явный x неприкосновенен, лог [lvn-slot] →
  LogShip) + 7 юнит-тестов. Полный EditMode 508/0 красных. Коммит 3b9b9ef.
  Первопричина — импортёр (→ G1); партнёрам нужен свежий APK.
- 2026-07-19 13:1x — тик — лестница A–F закрыта, дописан уровень G (G1–G7:
  импортёрские починки по ворклистам зонда, полный PlayMode-цикл, перф-
  бюджеты, мутационный соук, D3-сценарии, AltTester, live-проверка ночной
  джобы).
- 2026-07-19 12:30 — F3 — stability 10×: ФЛЕЙКОВ НЕТ (10/10 прогонов чистые,
  2490 соук-прохождений). Лестница A–F закрыта, кроме хвостов следующего
  уровня (полный C2-цикл в PlayMode, AltTester-сценарии D2, D3-скрипты,
  live-проверка ночной CI-джобы после push). Ответ на исходный вопрос
  стабильности: на текущем коде «раз на 10» ничего не отваливается.
- 2026-07-19 12:2x — B2-хвосты, C1/C2-ядро/C3, D2-lite, E1/E2 — второй заход
  «до полного завершения»: StageAndServiceOpTests (blur/obj/hotspot-GoTo/
  ext-опы, 5/5), BootSmokeTests (PlayMode: настоящий NovelApp против
  настоящего Go-сервера, «shell built» за 2с), qa/story-walk.sh (сюжетный
  бот тапами: 10/10 уникальных кадров главы LVN Tour на устройстве),
  ночная stability-джоба в ci.yml, гейт CI перестал валить Skipped.
  Финальный полный EditMode: 500 тестов, 494 зелёных, 0 красных, 6 skip.

- 2026-07-19 11:0x — A1+A2+A3+A4, B1, B2(input/load), D1b, F1, F2 — большой
  заход (юзер онлайн, «до полного завершения»): соук-бот по 83 скриптам ×3
  сида с resume-truth (35с), контракт-тесты input/load, инвентаризация опов,
  server-override для dev-APK (доказан end-to-end на :8099: online=True,
  14 тайтлов, monkey PASS), qa/run-all.sh + qa/stability.sh. Полный EditMode:
  495 тестов, 489 зелёных, 0 красных, 6 skip (известные контент-баги).
  ГЛАВНАЯ НАХОДКА: соук поймал 6 глав cold с бесконечным гардеробным
  goto-циклом (импортёр теряет условие выхода на пине) — история после
  цикла мёртвый код; кандидат в «партнёрский баг гардероба». Даблы
  wardrobe_show НЕ регистрировались: плеер честно ловит цикл предохранителем.
- 2026-07-19 09:35 — D1 — qa/monkey.sh написан и зелёный на живом APK
  (timeromance-demo.apk 20260719.0243, AVD Pixel_3a_API_34 arm64): бут до
  «veil handed off» за 5.5с, monkey 500/500 событий без крэшей, скриншоты
  подтверждают вход в главу. Диагностирован и задокументирован тройной
  капкан эмулятора (SwiftShader/Vulkan/immersive-фокус). Замечено на проде:
  KTX2-варианты части фонов отдают 404 (клиент корректно фолбэчится,
  «permanent 404 silenced»), фон Apartment_Katya_evening живёт на серой
  болванке — контент-гэп, не движок.
